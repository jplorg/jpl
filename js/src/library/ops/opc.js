/**
 * Sub operator for OP_COMPARISON
 *
 * { by: function }
 *
 * { by: [op] }
 */
export const OPC_EQUAL = '==';

/**
 * Sub operator for OP_COMPARISON
 *
 * { by: function }
 *
 * { by: [op] }
 */
export const OPC_GREATER = '>';

/**
 * Sub operator for OP_COMPARISON
 *
 * { by: function }
 *
 * { by: [op] }
 */
export const OPC_GREATEREQUAL = '>=';

/**
 * Sub operator for OP_COMPARISON
 *
 * { by: function }
 *
 * { by: [op] }
 */
export const OPC_LESS = '<';

/**
 * Sub operator for OP_COMPARISON
 *
 * { by: function }
 *
 * { by: [op] }
 */
export const OPC_LESSEQUAL = '<=';

/**
 * Sub operator for OP_COMPARISON
 *
 * { by: function }
 *
 * { by: [op] }
 */
export const OPC_UNEQUAL = '!=';
